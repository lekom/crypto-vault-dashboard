"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getScheduledTransferByJobId = exports.getScheduledTransfers = void 0;
const constants_1 = require("../../constants");
const query = `
    query ($smartAccount: String, $chainId: Int) {
      ScheduledTransfers_ExecutionQuery (where:{ smartAccount: { _eq: $smartAccount }, chainId: { _eq: $chainId }}) {
        id
        jobId
        smartAccount
        executeInterval
        numberOfExecutions
        numberOfExecutionsCompleted
        startDate
        isEnabled
      }
    }
  `;
const getScheduledTransfers = async ({ account, client, }) => {
    const variables = {
        smartAccount: account.address,
        chainId: await client.getChainId(),
    };
    const response = await fetch(constants_1.GLOBAL_CONSTANTS.INDEXER_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            query,
            variables,
        }),
    });
    const responseBody = await response.json();
    if (response.ok) {
        return responseBody.data.ScheduledTransfers_ExecutionQuery;
    }
    else {
        throw new Error(`Error: ${responseBody.errors.map((error) => error.message).join(', ')}`);
    }
};
exports.getScheduledTransfers = getScheduledTransfers;
const queryById = `
    query ($smartAccount: String, $jobId: numeric, $chainId: Int) {
      ScheduledTransfers_ExecutionQuery (where:{ smartAccount: { _eq: $smartAccount }, jobId: { _eq: $jobId }, chainId: { _eq: $chainId }}) {
        id
        jobId
        smartAccount
        executeInterval
        numberOfExecutions
        numberOfExecutionsCompleted
        startDate
        isEnabled
      }
    }
  `;
const getScheduledTransferByJobId = async ({ account, jobId, client, }) => {
    const variables = {
        smartAccount: account.address,
        jobId,
        chainId: await client.getChainId(),
    };
    const response = await fetch(constants_1.GLOBAL_CONSTANTS.INDEXER_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            query: queryById,
            variables,
        }),
    });
    const responseBody = await response.json();
    if (response.ok) {
        return responseBody.data.ScheduledTransfers_ExecutionQuery[0];
    }
    else {
        throw new Error(`Error: ${responseBody.errors.map((error) => error.message).join(', ')}`);
    }
};
exports.getScheduledTransferByJobId = getScheduledTransferByJobId;
//# sourceMappingURL=scheduledTransfers.js.map