import { Address, Hex } from 'viem';
import { Module } from '../types';
type Params = {
    executeInterval: number;
    numberOfExecutions: number;
    startDate: number;
    executionData: Hex;
    hook?: Address;
};
export declare const getScheduledTransfersExecutor: ({ executeInterval, numberOfExecutions, startDate, executionData, hook, }: Params) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map