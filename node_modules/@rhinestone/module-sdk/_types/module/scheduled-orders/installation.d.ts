import { Address, Hex } from 'viem';
import { Module } from '../types';
type Params = {
    chainId: number;
    executeInterval: number;
    numberOfExecutions: number;
    startDate: number;
    executionData: Hex;
    hook?: Address;
};
export declare const getScheduledOrdersExecutor: ({ chainId, executeInterval, numberOfExecutions, startDate, executionData, hook, }: Params) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map