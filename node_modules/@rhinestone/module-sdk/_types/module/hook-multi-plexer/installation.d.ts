import { Address, Hex } from 'viem';
import { Module } from '../types';
import { SigHookInit } from './types';
type Params = {
    globalHooks: Address[];
    valueHooks: Address[];
    delegatecallHooks: Address[];
    sigHooks: SigHookInit[];
    targetHooks: SigHookInit[];
    selector?: Hex;
    hookType?: number;
};
export declare const getHookMultiPlexer: ({ globalHooks, valueHooks, delegatecallHooks, sigHooks, targetHooks, selector, hookType, }: Params) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map