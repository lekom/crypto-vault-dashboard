import { Address, Hex, PublicClient } from 'viem';
import { Execution } from '../../account';
import { Account } from '../../account';
import { HookType } from './types';
export declare const getHooks: ({ account, client, }: {
    account: Account;
    client: PublicClient;
}) => Promise<Address[]>;
export declare const getAddHookAction: ({ hook, hookType, sig, }: {
    hook: Address;
    hookType: HookType;
    sig?: Hex;
}) => Execution;
export declare const getRemoveHookAction: ({ hook, hookType, sig, }: {
    hook: Address;
    hookType: HookType;
    sig?: Hex;
}) => Execution;
//# sourceMappingURL=usage.d.ts.map