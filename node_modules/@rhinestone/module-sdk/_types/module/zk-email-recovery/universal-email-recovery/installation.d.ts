import { Address, Hex } from 'viem';
import { Module } from '../../types';
export declare const getUniversalEmailRecoveryExecutor: ({ validator, isInstalledContext, initialSelector, guardians, weights, threshold, delay, expiry, chainId, hook, }: {
    validator: Address;
    isInstalledContext: Hex;
    initialSelector: Hex;
    guardians: Array<Address>;
    weights: Array<bigint>;
    threshold: bigint;
    delay: bigint;
    expiry: bigint;
    chainId: number;
    hook?: Address;
}) => Module;
//# sourceMappingURL=installation.d.ts.map