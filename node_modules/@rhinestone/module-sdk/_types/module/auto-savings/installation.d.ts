import { Address } from 'viem';
import { Module } from '../types';
type Params = {
    chainId: number;
    configs: {
        token: Address;
        percentage: bigint;
        vault: Address;
    }[];
    hook?: Address;
};
export declare const getAutoSavingsExecutor: ({ chainId, configs, hook, }: Params) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map