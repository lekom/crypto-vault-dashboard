import { Address, Hex, PublicClient } from 'viem';
import { Module } from '../types';
import { Account } from '../../account';
import { CallType } from '../../module/types';
type Params = {
    account: Account;
    client: PublicClient;
    waitPeriod: number;
    owner: Address;
    moduleType: 'hook' | 'fallback' | 'executor';
    hook?: Address;
};
export declare const getColdStorageHook: ({ account, client, waitPeriod, owner, moduleType, hook, }: Params) => Promise<Module>;
type FlashloanParams = {
    addresses: Address[];
    functionSig?: Hex;
    callType: CallType;
    selector?: Hex;
    hook?: Address;
};
export declare const getAllowedCallbackSenders: ({ addresses, functionSig, callType, selector, hook, }: FlashloanParams) => Module;
export {};
//# sourceMappingURL=installation.d.ts.map