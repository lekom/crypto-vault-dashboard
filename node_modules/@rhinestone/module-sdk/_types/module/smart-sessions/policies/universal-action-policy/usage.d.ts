import { Address, Hex, PublicClient } from 'viem';
import { ActionConfig } from './types';
export declare const getActionConfig: ({ client, configId, multiplexer, userOpSender, }: {
    client: PublicClient;
    configId: Hex;
    multiplexer: Address;
    userOpSender: Address;
}) => Promise<ActionConfig>;
//# sourceMappingURL=usage.d.ts.map