import { Address, Hex, PublicClient } from 'viem';
import { UsageLimitConfig } from './types';
export declare const getUsageLimitConfig: ({ client, configId, multiplexer, smartAccount, }: {
    client: PublicClient;
    configId: Hex;
    multiplexer: Address;
    smartAccount: Address;
}) => Promise<UsageLimitConfig>;
//# sourceMappingURL=usage.d.ts.map