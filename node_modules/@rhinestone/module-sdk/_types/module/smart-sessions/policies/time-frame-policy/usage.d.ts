import { Address, Hex, PublicClient } from 'viem';
import { TimeFrameConfig } from './types';
export declare const getTimeFramePolicyData: ({ client, configId, multiplexer, smartAccount, }: {
    client: PublicClient;
    configId: Hex;
    multiplexer: Address;
    smartAccount: Address;
}) => Promise<TimeFrameConfig>;
//# sourceMappingURL=usage.d.ts.map