import { Address } from 'viem';
import { Module } from '../types';
import { Session } from './types';
type Params = {
    sessions?: Session[];
    useRegistry?: boolean;
    hook?: Address;
};
export declare const getSmartSessionsValidator: ({ sessions, useRegistry, hook, }: Params) => Module;
export declare const getSmartSessionsCompatibilityFallback: () => Module;
export {};
//# sourceMappingURL=installation.d.ts.map