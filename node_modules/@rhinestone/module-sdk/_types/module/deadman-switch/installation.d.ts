import { Address, PublicClient } from 'viem';
import { Module } from '../types';
import { Account } from '../../account';
export declare const getDeadmanSwitch: ({ moduleType, nominee, timeout, hook, account, client, }: {
    nominee: Address;
    timeout: number;
    moduleType: "hook" | "validator";
    hook?: Address;
    account: Account;
    client: PublicClient;
}) => Promise<Module>;
//# sourceMappingURL=installation.d.ts.map