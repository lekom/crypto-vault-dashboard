import { encodeAbiParameters } from 'viem';
import { GLOBAL_CONSTANTS } from '../../constants.js';
export const getSocialRecoveryValidator = ({ threshold, guardians, hook, }) => {
    return {
        address: GLOBAL_CONSTANTS.SOCIAL_RECOVERY_ADDRESS,
        module: GLOBAL_CONSTANTS.SOCIAL_RECOVERY_ADDRESS,
        initData: encodeAbiParameters([
            { name: 'threshold', type: 'uint256' },
            { name: 'guardians', type: 'address[]' },
        ], [BigInt(threshold), guardians.sort()]),
        deInitData: '0x',
        additionalContext: '0x',
        type: 'validator',
        hook,
    };
};
//# sourceMappingURL=installation.js.map