import { abi } from './abi.js';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getTimeFramePolicyData = async ({ client, configId, multiplexer, smartAccount, }) => {
    return (await client.readContract({
        address: GLOBAL_CONSTANTS.TIME_FRAME_POLICY_ADDRESS,
        abi: abi,
        functionName: 'getTimeFrameConfig',
        args: [configId, multiplexer, smartAccount],
    }));
};
//# sourceMappingURL=usage.js.map