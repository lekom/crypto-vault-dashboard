import { abi } from './abi.js';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getActionConfig = async ({ client, configId, multiplexer, userOpSender, }) => {
    return (await client.readContract({
        address: GLOBAL_CONSTANTS.UNIVERSAL_ACTION_POLICY_ADDRESS,
        abi: abi,
        functionName: 'actionConfigs',
        args: [configId, multiplexer, userOpSender],
    }));
};
//# sourceMappingURL=usage.js.map