import { abi } from './abi.js';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getValueLimitConfig = async ({ client, configId, msgSender, userOpSender, }) => {
    return (await client.readContract({
        address: GLOBAL_CONSTANTS.VALUE_LIMIT_POLICY_ADDRESS,
        abi: abi,
        functionName: 'getValueLimit',
        args: [configId, msgSender, userOpSender],
    }));
};
//# sourceMappingURL=usage.js.map