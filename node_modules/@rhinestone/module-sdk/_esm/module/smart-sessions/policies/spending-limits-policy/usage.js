import { abi } from './abi.js';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getPolicyData = async ({ client, configId, multiplexer, token, userOpSender, }) => {
    return (await client.readContract({
        address: GLOBAL_CONSTANTS.SPENDING_LIMITS_POLICY_ADDRESS,
        abi: abi,
        functionName: 'getPolicyData',
        args: [configId, multiplexer, token, userOpSender],
    }));
};
//# sourceMappingURL=usage.js.map