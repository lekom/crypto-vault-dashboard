import { abi } from './abi.js';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getUsageLimitConfig = async ({ client, configId, multiplexer, smartAccount, }) => {
    return (await client.readContract({
        address: GLOBAL_CONSTANTS.USAGE_LIMIT_POLICY_ADDRESS,
        abi: abi,
        functionName: 'getUsageLimit',
        args: [configId, multiplexer, smartAccount],
    }));
};
//# sourceMappingURL=usage.js.map