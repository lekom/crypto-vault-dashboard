import { getAccountImplementation } from './getAccountImplementation.js';
export const encode1271Hash = ({ account, chainId, validator, hash, }) => {
    const accountImplementation = getAccountImplementation({ account });
    return accountImplementation.encode1271Hash({
        account: account.address,
        chainId,
        validator,
        hash,
    });
};
//# sourceMappingURL=encode1271Hash.js.map