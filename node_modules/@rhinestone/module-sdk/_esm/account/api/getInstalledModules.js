import { getAccountImplementation } from './getAccountImplementation.js';
export const getInstalledModules = async ({ client, account, moduleTypes, }) => {
    const accountImplementation = getAccountImplementation({ account });
    return accountImplementation.getInstalledModules({
        client,
        account,
        moduleTypes,
    });
};
//# sourceMappingURL=getInstalledModules.js.map