import { getAccountImplementation } from './getAccountImplementation.js';
export const isModuleInstalled = ({ client, account, module, }) => {
    const accountImplementation = getAccountImplementation({ account });
    return accountImplementation.isModuleInstalled({ client, account, module });
};
//# sourceMappingURL=isModuleInstalled.js.map