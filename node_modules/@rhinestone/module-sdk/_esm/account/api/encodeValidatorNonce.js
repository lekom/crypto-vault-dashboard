import { getAccountImplementation } from './getAccountImplementation.js';
export const encodeValidatorNonce = ({ account, validator, }) => {
    const accountImplementation = getAccountImplementation({ account });
    return accountImplementation.encodeValidatorNonce({
        validator,
    });
};
//# sourceMappingURL=encodeValidatorNonce.js.map