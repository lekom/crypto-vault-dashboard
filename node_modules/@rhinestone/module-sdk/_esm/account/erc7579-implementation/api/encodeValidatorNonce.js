import { pad } from 'viem';
import { isModule } from '../../../account/utils.js';
export const encodeValidatorNonce = ({ validator, }) => {
    return BigInt(pad(isModule(validator) ? validator.address : validator, {
        dir: 'right',
        size: 24,
    }));
};
//# sourceMappingURL=encodeValidatorNonce.js.map