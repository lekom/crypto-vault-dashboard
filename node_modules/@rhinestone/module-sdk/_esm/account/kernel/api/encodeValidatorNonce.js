import { pad, encodePacked } from 'viem';
import { isKernelModule } from './utils.js';
export const encodeValidatorNonce = ({ validator, }) => {
    return BigInt(pad(encodePacked(['bytes1', 'bytes1', 'address'], [
        '0x00',
        '0x00',
        isKernelModule(validator) ? validator.address : validator,
    ]), {
        dir: 'right',
        size: 24,
    }));
};
//# sourceMappingURL=encodeValidatorNonce.js.map