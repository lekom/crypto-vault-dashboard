import { GLOBAL_CONSTANTS } from '../../constants.js';
const query = `
  query ($chainId: Int) {
     Registry_ModuleRegistration (where: { chainId: { _eq: $chainId } }) {
        id
        implementation
      }
  }
`;
export const getRegistryModules = async ({ client, }) => {
    const variables = {
        chainId: await client.getChainId(),
    };
    const response = await fetch(GLOBAL_CONSTANTS.INDEXER_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            query,
            variables,
        }),
    });
    const responseBody = await response.json();
    if (response.ok) {
        return responseBody.data.Registry_ModuleRegistration;
    }
    else {
        throw new Error(`Error: ${responseBody.errors
            .map((error) => error.message)
            .join(', ')}`);
    }
};
//# sourceMappingURL=registry.js.map