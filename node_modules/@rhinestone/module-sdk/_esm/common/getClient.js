import { createPublicClient, http } from 'viem';
export const getClient = ({ rpcUrl }) => {
    return createPublicClient({
        transport: http(rpcUrl),
    });
};
//# sourceMappingURL=getClient.js.map