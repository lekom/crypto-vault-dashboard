import type { Address } from "viem";
type MerklReward = {
    chain: number;
    token: {
        address: string;
        chainId: number;
        symbol: string;
        decimals: number;
        price: number;
    };
    amount: string;
    claimed: string;
    pending: string;
};
export declare function getMerklRewardsData(campaignId: string[], chainId: number, account: Address): Promise<MerklReward[]>;
export declare function getMerklStakingApr(campaignId: string): Promise<number>;
export {};
//# sourceMappingURL=common.d.ts.map